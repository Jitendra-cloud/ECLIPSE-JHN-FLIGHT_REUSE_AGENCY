projection;
strict ( 2 );
use draft;
extensible;

define behavior for ZJS_C_AGENCYTP alias Agency
extensible
{
  use create;
  use update;
  use delete;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}